# macOS
.DS_Store

# dependências e build
node_modules/
.next/
out/

# ambiente (segredos)
.env
.env.local
.env*.local

# logs
*.log

# autenticação (senhas reais e segredo de sessão ficam fora do Git)
permissoes/usuarios.json
operacao/.segredo-auth

# material de teste gerado (npm run pendencias:teste) / dados reais da operação
operacao/pendencias.json

# saídas geradas pelo graphify (regenerável com /graphify)
graphify-out/
